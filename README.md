# Verb package registry

Flat APT repository for Verb. Add with:

    deb [trusted=yes] https://ShaileshRawat1403.github.io/verb/apt/ ./

Rebuild with `python3 runtime/scripts/build-verb-apt-repo.py --out apt/`.
