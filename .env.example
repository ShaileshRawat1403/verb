# Verb does not read provider credentials from this file or package them at build time.
# Optional Assistant provider credentials are entered in the Android app and stored with the
# platform Keystore. Keep local secrets in ignored files; never add them here.
