sed -i 's|override fun onCopyTextToClipboard(session: TerminalSession, text: String) {|override fun onCopyTextToClipboard(session: TerminalSession, text: String) {\n        terminalView?.context?.let { ctx ->\n            val clipboard = ctx.getSystemService(Context.CLIPBOARD_SERVICE) as ClipboardManager\n            clipboard.setPrimaryClip(ClipData.newPlainText("Termux Selection", text))\n        }\n    }\n\n    override fun onPasteTextFromClipboard(session: TerminalSession?) {\n        val s = session ?: this.session\n        terminalView?.context?.let { ctx ->\n            val clipboard = ctx.getSystemService(Context.CLIPBOARD_SERVICE) as ClipboardManager\n            val clipData = clipboard.primaryClip\n            if (clipData != null \&\& clipData.itemCount > 0) {\n                val text = clipData.getItemAt(0).coerceToText(ctx).toString()\n                s?.write(text)\n            }\n        }\n    }\n\n    // Remove old onPasteTextFromClipboard\n    /*|g' app/src/main/java/com/example/verb/terminal/TermuxTerminalRuntimeAdapter.kt
